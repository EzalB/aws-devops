{{- if .Values.workloadIdentity.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    iam.gke.io/gcp-service-account: {{ .Values.workloadIdentity.google_service_account }}
  name: {{ template "obm.appName" . }}
{{- end }}
